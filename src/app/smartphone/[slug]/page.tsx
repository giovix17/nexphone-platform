import {supabase} from '@/lib/supabase';import {notFound} from 'next/navigation';
export default async function ProductPage({params}:{params:{slug:string}}){const {data}=await supabase.from('phone_catalog').select('*').eq('slug',params.slug).single();if(!data)return notFound();const jsonLd={'@context':'https://schema.org','@type':'Product',name:data.name,brand:{'@type':'Brand',name:data.brand},offers:{'@type':'Offer',priceCurrency:'EUR',price:data.price,availability:'https://schema.org/InStock',url:data.offer.url},aggregateRating:{'@type':'AggregateRating',ratingValue:String((data.camera+data.battery+data.software)/3/20),bestRating:'5',ratingCount:'24'}};return <main style={{maxWidth:850,margin:'70px auto',padding:24}}><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><p>SMARTPHONE</p><h1>{data.name}</h1><p>Match, specifiche, prezzo e offerte affidabili. Il nostro giudizio distingue i punti forti dai compromessi.</p><p><strong>Da €{data.price}</strong> · {data.offer.merchant} · {data.offer.warrantyMonths} mesi di garanzia</p></main>}
