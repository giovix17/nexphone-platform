import type {Metadata} from 'next';
export function generateMetadata({params}:{params:{intent:string;budget:string}}):Metadata{const intent=params.intent.replaceAll('-',' ');return {title:`Migliori smartphone per ${intent} sotto ${params.budget}€ | Nexphone`,description:`Scopri gli smartphone più adatti per ${intent}, con un budget massimo di ${params.budget} euro.`}}
export default function Landing({params}:{params:{intent:string;budget:string}}){const intent=params.intent.replaceAll('-',' ');return <main style={{maxWidth:850,margin:'70px auto',padding:24}}><p style={{color:'#0d6b4d',fontWeight:700}}>GUIDA PERSONALIZZATA</p><h1>I migliori smartphone per {intent} sotto {params.budget}€</h1><p>Una selezione aggiornata, con priorità, compromessi e offerte verificate. Imposta il tuo profilo per un ranking davvero personale.</p><a href="/" style={{color:'#0d6b4d',fontWeight:700}}>Trova il tuo match →</a></main>}
